// Deleted: content moved to `locals.tf` (defaults and examples migrated)
// If you need to override, set the variables in your environment or pass a
// `.tfvars` explicitly when running `terraform plan`/`apply`.
